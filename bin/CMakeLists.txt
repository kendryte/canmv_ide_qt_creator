if (NOT APPLE)
  # OPENMV-DIFF #
  # install(PROGRAMS qtcreator.sh DESTINATION bin)
  # OPENMV-DIFF #
  # install(PROGRAMS openmvide.sh DESTINATION bin)
  # OPENMV-DIFF #
  install(PROGRAMS canmvide.sh DESTINATION bin)
endif()
